#include<stdio.h>
#include<string.h>
int main()
{
int key;
char a[50];
printf("enter plain text:");
gets(a);
printf("enter key:");
scanf("%d",&key);
printf("caesar cipher is:");
for(int i=0;i<strlen(a);i++){
printf("%c",a[i]+key);
}
}
